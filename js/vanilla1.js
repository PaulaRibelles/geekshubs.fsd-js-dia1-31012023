
// Pide un nombre y muestra un mensaje de bienvenida con ese nombre

let nombre = 0

    nombre = prompt("Dime cuál es tu nombre");

    prompt("Bienvenido/a", nombre);

    // Lee un número por teclado e indica si es divisible entre 2 (resto = 0). Si no lo es,
// también debemos indicarlo.

let num = 0

parseInt(prompt("Inserta un número"));

// if (num % 2 === 0) {
//     console.log(prompt("Este número es divisibles entre dos"))? (
//         console.log(prompt("Este número no es divisible entre dos"));
//     );
// }

// Lee un número por teclado que pida el precio de un producto (puede tener
// decimales) y calcule el precio final con IVA. El IVA sera una constante que sera
// del 21%

let num2 = 0

const iva = 1.21 

resultado = 0


    // num2 = console.log("Introduce el valor del importe");
    // resultado = num2 / iva

    // console.log(prompt("El precio tatal es", resultado));



    // Muestra los números del 1 al 100 (ambos incluidos). Usa un bucle while.

    numAleatorio = 0

    while (numAleatorio === 0){
        console.log(numAleatorio ++)
    }